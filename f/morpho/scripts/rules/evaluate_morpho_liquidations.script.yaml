summary: Deduplicate Morpho liquidation transactions and save MonitorState
lock: '!inline f/morpho/scripts/rules/evaluate_morpho_liquidations.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    inputs: {type: object, format: resource-record}
    user: {type: string, pattern: '^0x[0-9a-fA-F]{40}$'}
    notify_on_first_run: {type: boolean, default: false}
  required: [inputs, user]
