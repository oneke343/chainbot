summary: Evaluate Morpho market risk and save MonitorState
lock: '!inline f/morpho/scripts/rules/evaluate_morpho_market_risk.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    inputs: {type: object, format: resource-record}
    market_ids:
      type: array
      items: {type: string, pattern: '^0x[0-9a-fA-F]{64}$'}
    default_rules: {type: object, default: {}, format: resource-record}
    market_rules: {type: object, default: {}, format: resource-record}
  required: [inputs, market_ids]
