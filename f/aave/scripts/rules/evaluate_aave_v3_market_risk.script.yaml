summary: Evaluate Aave V3 reserve risk and save MonitorState
lock: '!inline f/aave/scripts/rules/evaluate_aave_v3_market_risk.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    inputs: {type: object, format: resource-record}
    default_rules: {type: object, default: {}, format: resource-record}
    market_rules: {type: object, default: {}, format: resource-record}
  required: [inputs]
